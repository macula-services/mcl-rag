{application, rag, [
    {description, "Shared root + notation for the mcl-rag service's umbrella apps"},
    {vsn, "0.1.0"},
    {registered, [rag_store]},
    {mod, {rag_app, []}},
    {applications, [
        kernel,
        stdlib,
        crypto,
        cowboy,
        jsx,
        barrel,
        %% The store's document half; declared by this service since mcl_om 0.27.
        barrel_docdb,
        %% rag_embed_mcl_embedder implements its barrel_embed_provider.
        barrel_embed
    ]},
    {env, []},
    {modules, []},
    {licenses, ["Apache-2.0"]}
]}.
